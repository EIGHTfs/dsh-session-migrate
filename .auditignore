# 审计豁免（不影响 git 跟踪，仅跳过审计扫描）
# 语法同 .gitignore；用于「要入库但不该被自家规则审」的内容。

# 浏览器半侧单文件入口：DSH 只读 package.json exports["./client"] 指向的这一个文件，
#   官方插件同样是「源码分多文件 + 打包成单文件」——本仓库硬约束是不拆文件、不引入构建链。
#   800 行由段落注释与小函数承担；嵌套与行数偏高是单文件架构的固有代价，不是缺陷。
#   （与 dsh-git-push 对 lib/client.js 的豁免理由一致）
lib/client.js
